<%= form_with(model: eg_company, local: true) do |form| %>
  <% if eg_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eg_company.errors.count, "error") %> prohibited this eg_company from being saved:</h2>

      <ul>
      <% eg_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
