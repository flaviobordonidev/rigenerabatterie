<%= form_with(model: eg_user, local: true) do |form| %>
  <% if eg_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eg_user.errors.count, "error") %> prohibited this eg_user from being saved:</h2>

      <ul>
      <% eg_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :encrypted_password %>
    <%= form.text_field :encrypted_password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
